# Experiments

- `semantic-anchor-adapter/`: TypeScriptのselectionとsymbol-pathの往復変換を検証した初期スパイク
- `spotlight-extension/`: エディタ装飾とホップ遷移を検証した初期VS Code拡張

製品コードは`src/`、ツアー用ソースは`examples/`にあります。ここにあるコードは
設計判断の記録であり、配布VSIXには同梱しません。
